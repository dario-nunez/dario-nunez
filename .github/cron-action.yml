name: "Log every 5 minutes"
on:
  schedule:
  - cron: "*/5 * * * *"

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
    - name: Print a log message
      run: echo "This action runs every 5 minutes."
